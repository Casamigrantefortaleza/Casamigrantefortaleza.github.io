$(document).ready(function() {
    $('.carousel').slick({
      arrows: false, // Adicione setas de navegação (opcional)
      dots: true,   // Adicione indicadores (pontos) de navegação (opcional)
      autoplay: true, // Inicie a reprodução automática (opcional)
      autoplaySpeed: 3000, // Defina a velocidade da reprodução automática (opcional)
      infinite: true, // Repetição infinita (opcional)
      speed: 500, // Velocidade da animação (opcional)
      slidesToShow: 1, // Quantidade de slides visíveis (opcional)
    });
  });
  